<h1>Write a new post!</h1>
<% if @errors %>
    <ul>
      <% @errors.each do |error| %>
          <li><%= error %></li>
      <% end %>
    </ul>
<% end %>


<%= render partial: 'libraries' %>

<div class="row">
  <%= form_for :posts, url: posts_path, html: {id: 'post-form'}, class: 'col s12' do |f| %>
  <%= render partial: 'post_form', locals: {post: @post, f: f} %>
  <% end %>
</div>